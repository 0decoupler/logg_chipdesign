# Day 025 - 18-08 <!-- DD-MM -->

**Stage:** F6 <!-- F1 | F2 | F3 | F4 | F5 | F6 -->
**Topic:** minirv <!-- e.g. priority encoder / two's complement overflow / sCPU decode -->
**Time:** <!-- 1h 30m -->

---

## Notes

today, started early 4:30 in the morning for an hour of study ;)

RTFM:
> 1. What is the bit width of the PC register?
2. How many GPRs are there in total? What is the bit width of each GPR?
3. What are the differences between R[Ø] and R[Ø] in sISA?
4. What is the bit width of instruction encoding? How many basic formats do instructions have?
5. In the basic format of instructions, how many bits are needed to represent a GPR? Why?
6. What is the specific format of the add instruction?
7. There is another base instruction set called RV32E. What is the difference between it and RV32I?

my RTMF research:
1. In order to express 40 unique or base 38 instructions, we need PC register bit width of 6 bits, as it's in the range of `1-2^6` or `1-64`. Some room for the remaining 24 empty instruction space.
> i judged too early on this one. It was defined in the memory address along with 32 GPRs as a special-purpose register with 32-bit width. Gotta be mindful of it.
2. There are 32 GPRs and one special-purpose register for PC. Each is 32-bits wide, so we can fit a decimal value of about 4 million if it's unsigned integer register.
3. i didnt get this question cuz they're both the same in writing and have no distinct differences in any way. But `R[0]` essentially means very first GPR or `r0`.
4. Fixed 32-bits in length. R/S/I/U, 4 of core instr.
5. 5-bits. We have a total of 32 GPRs, so to locate one, we need 2^5 one-hot code bit lines.
6. `add` instruction has 3 other instructions in sequence:
```
 add t0, t1, t2; bltu t0, t1, overflow
``` 
there are other specific cases for both signed and unsigned additions to avoid overflow or subtraction formats.

7. it's the reduced version of the `I` version of RV32. The number of integer register were reduced to 16, which is primarily designed for embedded mcu with smaller base core.



### Concepts
<!-- Definitions, rules, formulas. Write them in my own words, not copied. -->
> *The RISC-V ISA keeps the source (rs1 and rs2) and destination (rd) registers at the same position in all formats to simplify decoding*
- i liked this thing, cuz if, say, `rd` is somewhere at left-most bits of the instruction for once, and it's located at a different bit-range, it's gonna be real-work to decode that stuff, case-by-case, yk.

### Diagram / waveform
<!-- Paste an image, ASCII schematic, or link to assets/day000-*.png -->

